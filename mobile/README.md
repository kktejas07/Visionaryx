# Mobile codebase has moved

The single React Native + React Native Web codebase now lives at `/app/frontend/`.
- Run web (dev): `cd /app/frontend && yarn start` (Expo Web on port 3000)
- Run iOS/Android: `cd /app/frontend && yarn ios` / `yarn android`
